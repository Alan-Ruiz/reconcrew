<%= simple_form_for [@location, @locationextras] do |f| %>
  <%= f.association :extra %>

 <script>
var form = document.querySelect("form-group");
function handleForm(event) { event.preventDefault(); }
form.addEventListener('submit', handleForm);
</script>

  <%= f.submit "add a extra", class: "btn btn-danger submit-bttn" %>
<% end %>

